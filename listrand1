clc
for x = rand(1,5)
if x > 0.5
fprintf('Random number %f is large.\n',x)
else
fprintf('Random number %f is small.\n',x)
end
end
Random number 0.964889 is large.
Random number 0.157613 is small.
Random number 0.970593 is large.
Random number 0.957167 is large.
Random number 0.485376 is small.
diary off
